<?php
// $sql =
//     "SELECT `product`.pro_name , `order_details`.quantity,`order_details`.unitprice , `order`.order_date
// FROM `product`, `order_details` , `order`WHERE `product`.id=`order_details`.pro_id
// AND `order_details`.order_id=`order`.order_id
// AND `order`.user_id=:user_id";
// $stmt = $link->prepare($sql);
// $stmt->bindParam(":user_id",  $_SESSION['userid'], PDO::PARAM_STR);
// $stmt->execute();
// $st = $stmt->fetch();
